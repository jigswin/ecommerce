spring:
  application:
    name: product-service

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ecommerce
    username: root
    password: 123456789

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081